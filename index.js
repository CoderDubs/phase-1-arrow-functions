const divide = () => {
    const result = 2000 / 100;
    return result;
}
const square = (base) => {
    const result = base * base;
    return result;
}
const add = (num1, num2) => {
    const result = (num1 + num2);
    return result;
}